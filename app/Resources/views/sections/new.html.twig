{% extends 'admin.html.twig' %}

{% block body %}
    <h1>Creer une nouvelle section</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
 <div>
        <button type="button" class="btn btn-success btn-rounded waves-light waves-effect w-md"  style="display: block; margin: 0 auto;">Creer nouvelle section</button>

    </div>
    {{ form_end(form) }}
        

    <a href="{{ path('sections_index') }}"class="btn btn-success btn-rounded waves-light waves-effect w-md">Retour a la liste</a>

    
 
{% endblock %}
{% block javascripts %}
    <script>
$(document).ready( function () {

  $(".chosen-select").chosen({});
});

    </script>
        {% endblock %}