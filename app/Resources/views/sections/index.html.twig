 {% extends 'admin.html.twig' %}

{% block body %}
    <h1>Liste des sections</h1>
 <div class="row">
 
                            <div class="col-12">
                                <div class="card-box table-responsive">
                                    <h4 class="m-t-0 header-title"><b>Table champs</b></h4>
                                    <p class="text-muted font-14 m-b-30">
 

                                    <table id="datatable" class="table table-bordered">
                                        <thead>
            <tr>
                <th>Id</th>
                <th>Nomsection</th>
                <th>Actions</th>
            </tr>
        </thead>

 <tbody>
        {% for section in sections %}
            <tr>
            
            


                <td><a href="{{ path('sections_show', { 'id': section.id }) }}">{{ section.id }}</a></td>
                <td>{{ section.nomSection }}</td>
                <td>
                    <ul>
                        
                            <a href="{{ path('sections_show', { 'id': section.id }) }}">
                            <i class="fi-eye"></i></a>
                        <br>
                         <a href="{{ path('sections_edit', { 'id': section.id }) }}">
                            <i class=" dripicons-pencil"></i></a>
                        
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div> <!-- end row -->


    <div >
        
            <a href="{{ path('sections_new') }}"class="btn btn-success btn-rounded waves-light waves-effect w-md">Creer nouvelle section</a>
   
    </div>
{% endblock %}

